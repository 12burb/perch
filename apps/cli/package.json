{
  "name": "@perch/cli",
  "version": "0.3.0",
  "private": true,
  "description": "The perch binary: init, dev (laptop mode), runner connect, doctor, backup, restore, migrate (spec \u00a72).",
  "license": "AGPL-3.0-only",
  "type": "module",
  "bin": {
    "perch": "./src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts",
    "./laptop": "./src/laptop.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@electric-sql/pglite-pgvector": "0.0.9",
    "@perch/api": "workspace:*",
    "@perch/connect": "workspace:*",
    "@perch/db": "workspace:*",
    "@perch/runner": "workspace:*",
    "@perch/vault": "workspace:*"
  }
}
