{
  "name": "@perch/scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Repository scripts: release, sbom, perf audit, seed, and the repo invariant tests.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "yaml": "2.9.1"
  }
}
