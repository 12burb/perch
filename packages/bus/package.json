{
  "name": "@perch/bus",
  "version": "0.0.2",
  "private": true,
  "description": "Bus interface, in-process implementation with per-topic replay buffers (Redis adapter in Phase 5).",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@perch/events": "workspace:*"
  }
}
