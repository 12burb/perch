{
  "name": "@perch/jobs",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres job queue: SKIP LOCKED, retries with backoff, cron via croner.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@perch/db": "workspace:*",
    "croner": "10.0.1",
    "drizzle-orm": "0.45.2"
  }
}
