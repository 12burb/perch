# @perch/engines

The Engine interface (spec §3.3), the registry, the fake engine, and the bridge for engines hosted
on a runner; adapters (acp, opencode, cli-harness, native, hermes) join with their tasks. See
[`docs/sessions.md`](../../docs/sessions.md).
