{
  "name": "@perch/connectors",
  "version": "0.0.0",
  "private": true,
  "description": "Connector manifests (manifest.yaml per provider) and the connectors.json index (spec §5.5).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
