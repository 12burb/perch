# TASKS

The queue. Take the first unchecked task whose prerequisites are checked (spec §11; one task per branch, one
branch per PR). Phases 3 and 4 are written at the phase gates from spec §10.

Legend: `[ ]` open · `[~]` in progress (on `main`, ADR-0065) · `[x] <commit or PR>` landed on `main`.

## Phase 0 — Foundation

Every task below and D.1 ship together in [PR #1](https://github.com/12burb/perch/pull/1) (one branch for
the phase, ADR-0018); each line names the commit that carries its evidence.

- [x] **0.1** Repo scaffold: Bun workspaces, Turborepo, Biome, strict TS, Changesets, Renovate, PR and issue templates, LICENSE (AGPL-3.0) + MIT licenses in bot-sdk, ui, events, api-client, connectors, templates (CI green on the empty monorepo) — commit `feat(scaffold): bun workspaces, turborepo, biome, strict ts, changesets, renovate` (ADR-0018)
- [x] **0.2** Governance files per §9.2 (all present; DCO check enforced) — commit `docs(governance): readme, contributing, conduct, security, governance, pledge, dco check`
- [x] **0.3** Dependency resolution: exact package names and versions for §2 from official docs, pinned; DECISIONS entry per non-obvious pick (lockfile committed) — commit `chore(deps): resolve and pin the §2 stack` (ADR-0019..0028)
- [x] **0.4** Spikes from §9.3, one PR each, outcomes in DECISIONS.md ∥ (every spike has a recorded pass or fallback) — commit `feat(spikes): phase 0 spikes with recorded outcomes` (ADR-0029..0038; `spikes/README.md`)
  - [x] **0.4.1** PTY on Bun — fallback: bun-pty (ADR-0029)
  - [x] **0.4.2** ACP handshake — pass (ADR-0030)
  - [x] **0.4.3** OpenCode SDK — pass, streamed reply gated on a key (ADR-0031)
  - [x] **0.4.4** PGlite — pass (ADR-0032)
  - [x] **0.4.5** QuickJS sandbox — pass (ADR-0033)
  - [x] **0.4.6** better-auth on Bun — pass at the HTTP level; browser passkeys in 0.8 (ADR-0034)
  - [x] **0.4.7** dockerode from Bun — passes in CI; execs never hijack the connection (ADR-0035)
  - [x] **0.4.8** Caddy wildcard — deferred; path mode is the default (ADR-0036)
  - [x] **0.4.9** Preview tunnel over a local runner — pass (ADR-0037)
  - [x] **0.4.10** cloudflared profile — deferred; Tailscale documented (ADR-0038)
- [x] **0.5** packages/db: Drizzle schema for identity, tenancy, projects, runners, channels, messages, files, instance_settings; Db factory for postgres and pglite; migrations; PGlite test harness (schema tests pass on both drivers) — commit `feat(db): drizzle schema, db factory, embedded migrations, pglite harness` (ADR-0039, ADR-0040)
- [x] **0.6** packages/events, bus, jobs, vault with unit tests (queue survives worker crash, cron fires, vault round-trips and rotates) — commit `feat(core): events catalog, in-process bus, postgres job queue, vault` (ADR-0041)
- [x] **0.7** apps/api skeleton: Hono + zod-openapi, error model, request logging, health, version, OpenAPI at /api/openapi.json, generated TS client (GET /api/health typed end to end) — commit `feat(api): hono skeleton with openapi, error model, request logging, typed client` (ADR-0042, ADR-0043)
- [x] **0.8** Auth: better-auth with Drizzle adapter, email+password, passkeys, generic OIDC, users profile row, invites, api_tokens (Playwright: sign up, sign in with passkey, invite accepted) — commit `feat(auth): better-auth with passkeys and OIDC, profiles, invites, api tokens` (ADR-0044..0050; `bun run e2e`: e2e/auth.e2e.ts, 6 passed at 1440 px and 390 px)
- [x] **0.9** Workspaces, memberships, RBAC, authorize() middleware, audit log via bus (member cannot read another workspace; audit rows appear) — commit `feat(workspaces): rbac with authorize(), members, and the audit log via the bus` (ADR-0051..0053; apps/api/test/workspaces.test.ts, packages/policy/test/authorize.test.ts)
- [x] **0.10** WS server: subscribe, resume with seq, presence, typing (two tabs see each other's presence; reconnect replays) — commit `feat(ws): /api/ws with subscribe, resume, presence, and typing` (ADR-0054; apps/api/test/ws.test.ts; e2e/presence.e2e.ts)
- [x] **0.11** packages/ui: tokens, dark and light themes, shadcn base, Shell, Rail, Sidebar, Panel, Drawer, CommandPalette, Peek, Composer skeleton (Playwright component tests; axe passes) — commit `feat(ui): tokens, themes, shadcn base, and the shell components with component tests` (ADR-0055; `bun run ct`: packages/ui/src/**/*.ct.tsx)
- [x] **0.12** apps/web shell: routes for the six rail tabs, mobile tab bar, empty states, profile and workspace settings (390 px and 1440 px screenshots in the PR) — commit `feat(web): the shell with six modes, mobile tab bar, empty states, and settings` (ADR-0056; e2e/shell.e2e.ts; screenshots in docs/screenshots/0.12)
- [x] **0.13** Deploy: Dockerfile.api, Dockerfile.runner base, compose, Caddyfile, `perch init`, setup wizard (admin, workspace, PERCH_PUBLIC_URL, telemetry checkbox) (fresh Ubuntu VM: docker compose up → wizard → sign in) — commit `feat(deploy): dockerfiles, compose, caddy, perch init, and the setup wizard` (ADR-0057, ADR-0058; apps/api/test/setup.test.ts, apps/cli/test/init.test.ts, e2e/00-setup.e2e.ts; the compose smoke on a fresh VM runs in CI, task 0.15)
- [x] **0.14** apps/cli skeleton: perch dev on PGlite with in-process runner stub, perch doctor, perch backup|restore (laptop smoke test in CI on Linux, macOS, Windows) — commit `feat(cli): perch dev on PGlite with the in-process runner, doctor, backup, and restore` (ADR-0059; apps/cli/test/laptop.test.ts is the laptop smoke; the macOS and Windows legs run in CI, task 0.15)
- [x] **0.15** CI pipeline per §8 including multi-arch image publish on tag and cosign (a tagged pre-release publishes signed images) — commit `ci: the §8 pipeline, weekly CodeQL, changesets, and the signed multi-arch release` (ADR-0060; .github/workflows/{ci,codeql,changesets,release}.yml; a tagged pre-release is exercised by the release workflow itself)

## Phase 1 — IDE core

- [x] **1.1** apps/runner control channel: register, heartbeat, capability token verification, hosted mode in the runner image (a launched runner registers within 5 s) — commit `feat(runner): the control channel` (ADR-0066; apps/api/test/runner-channel.test.ts, apps/runner/test/client.test.ts)
- [x] **1.2** Supervisor: dockerode lifecycle, limits, idle stop, per-user home volumes, shared mode (two workspaces get two containers; idle stop works) — commit `feat(api): the supervisor` (ADR-0067; apps/api/test/supervisor.test.ts, apps/api/test/supervisor.docker.test.ts)
- [x] **1.3** Local runner: perch runner connect with a minted token, owner-only access, Environments page (a laptop registers and shows online from a phone) — commit `feat(runner): local runners and the Environments page` (ADR-0068; e2e/environments.e2e.ts, apps/api/test/runners-api.test.ts)
- [x] **1.4** Projects: create empty, upload, clone via HTTPS token or SSH deploy key; project volume; .perch/project.json read and validated; devcontainer.json honored (clone a public repo; defaults applied) — commit `feat(projects): projects on a runner, clones with a token or the deploy key` (ADR-0069; e2e/projects.e2e.ts clones github.com/octocat/Hello-World, apps/api/test/projects-api.test.ts, apps/runner/test/projects.test.ts)
- [x] **1.5** Runner fs, git, ports, exec methods with policy hooks (fs.search under 200 ms on a 50k-file repo) — commit `feat(runner): fs, git, ports, and exec methods behind one policy hook` (ADR-0070; apps/runner/test/fs.test.ts searches a 50,000-file tree in ~110–140 ms on ripgrep; apps/runner/test/{policy,git,ports,exec}.test.ts; apps/api/test/runner-ports.test.ts)
- [x] **1.6** Code mode: file tree, EditorGroup with CodeMirror 6, tabs, breadcrumbs, search/replace, markdown and image preview (open, edit, save, reopen) — commit `feat(editor): Code mode file tree and CodeMirror editor with previews` (ADR-0071; e2e/editor.e2e.ts opens, edits, saves, reloads, and reopens a file and previews markdown and an image; packages/ui/src/components/editor-group.ct.tsx; apps/api/test/project-fs.test.ts)
- [x] **1.7** Terminal: pty.open streams, xterm.js drawer, tmux persistence, per-user shells, path links open in the editor (reload keeps the shell) — commit `feat(terminal): xterm drawer over pty streams with tmux persistence` (ADR-0073; e2e/terminal.e2e.ts runs a command, reloads into the same shell with its scrollback, and opens a printed path in the editor; apps/runner/test/pty.test.ts; apps/api/test/terminal.test.ts)
- [x] **1.8** packages/engines: Engine interface, EngineEvent persistence, session lifecycle, session_events replay endpoint (unit tests with a fake engine) — commit `feat(sessions): engine interface, session lifecycle, and transcript replay` (ADR-0074; packages/engines/test/{fake,runner-engine}.test.ts; apps/api/test/sessions.test.ts drives the fake engine through open → turn → permission → cancel → error → replay with monotonic seqs and WS fan-out)
- [x] **1.9** ACP adapter: spawn registry agents, map events, permissions, modes, MCP passthrough for Perch tools (Gemini CLI and Codex complete a two-turn session with one permission prompt) — commit `feat(acp): registry agents as engines on every runner` (ADR-0075; apps/runner/test/acp.test.ts completes two turns with one permission prompt against a registry-shaped agent, and against Gemini CLI or Codex with `PERCH_ACP_TEST_AGENT` and credentials; apps/api/test/sessions-acp.test.ts runs the same through the api; MCP passthrough carries the project's servers until the gateway of task 1.17)
- [x] **1.10** OpenCode adapter: opencode serve per project, SDK client, plan/build, subagents (a session edits a file and the diff arrives as EngineEvents) — commit `feat(opencode): OpenCode sessions on a server per project` (ADR-0076; apps/runner/test/opencode.test.ts: a session edits a file and the edit tool's diff arrives on its tool_result, a side change as a diff tool call from the session diff; plan/build as the prompt's agent; permissions, abort, provider errors; the pinned binary in the runner image)
- [x] **1.11** cli-harness adapter behind a feature flag: Codex exec --json and Claude Code -p --output-format stream-json (flag off; both work in a local runner under the owner's login) — commit `feat(cli-harness): official CLIs as an engine on local runners, behind a flag` (ADR-0077; apps/runner/test/cli-harness.test.ts drives stand-ins for both CLIs through two resumed turns; apps/api/test/cli-harness.test.ts covers the flag gate and the lane end to end; the flag is off by default and hosted runners refuse the engine)
- [x] **1.12** Session pane: Composer in session mode, streaming transcript, tool cards, PermissionPrompt, usage footer, session list, fork, rename (Playwright: plan → build → permission → done) — commit `feat(sessions): the session pane with transcript, permissions, usage, fork, and rename` (ADR-0078; e2e/session.e2e.ts runs plan → build → permission → done on the fake ACP agent, then renames and forks; packages/ui/src/components/session-transcript.ct.tsx; apps/web/test/transcript.test.ts; apps/api/test/sessions.test.ts covers rename and fork)
- [x] **1.13** DiffView: per-turn and cumulative diffs, hunk accept/reject, Accept all / Reject all, Restore checkpoint, Apply on code blocks (accept two hunks, reject one, restore turn 1) — commit `feat(diff): per-turn diffs, hunk accept and reject, checkpoints, and restore` (ADR-0079; e2e/diff.e2e.ts accepts two hunks, rejects one, applies a code block and restores turn 1; apps/runner/test/checkpoints.test.ts; apps/api/test/sessions-diff.test.ts; packages/events/test/unified-diff.test.ts; packages/ui/test/code-blocks.test.ts; packages/ui/src/components/diff-view.ct.tsx)
- [x] **1.14** ⌘K inline edit in the editor (select, instruct, diff in place, accept) — commit `feat(editor): ⌘K rewrites a selection, with the diff in place` (ADR-0080; e2e/inline-edit.e2e.ts selects, instructs, accepts and saves, then rejects and gets the original back, at both viewports; apps/api/test/sessions-inline.test.ts; packages/events/test/code-blocks.test.ts)
- [x] **1.15** Brains v1: credentials vault UI, model profiles, catalog seeded from models.dev, endpoint providers including Ollama auto-detect, model picker (an OpenAI key and an Ollama endpoint both run a session) — commit `feat(brains): bring your own models — credentials, brains, and a live catalog` (ADR-0081, which replaces the models.dev seed with each provider's own live listing; e2e/brains.e2e.ts adds a key and an endpoint, tests both, names two brains, and runs a session on each; apps/api/test/brains.test.ts proves the key reaches only the engine's environment; packages/gateway/test/catalog.test.ts; packages/policy/test/authorize.test.ts)
- [x] **1.16** Connections v1: packages/connect core, manifest schema, token paste, OAuth callback route, CIMD document, GitHub App wizard and connector (clone via GitHub connection → PR opened) — commits `feat(connect): connector manifests, the token and GitHub App lanes, and the CIMD document`, `feat(connect): the OAuth lane, and a pull request opened on a connection`, `feat(connect): the Connections card and the GitHub App wizard` (ADR-0082; apps/api/test/connections.test.ts clones over authenticated smart HTTP with a minted installation token, commits, pushes, and opens the PR, asserting every git-receive-pack carried that token; packages/connect/test/connect.test.ts verifies the app JWT against a generated key; e2e/connections.e2e.ts covers the card and the wizard's prefilled URLs)
- [x] **1.17** MCP gateway skeleton: /mcp/{connectionId} proxy with token injection, allow-lists, audit; GitHub's MCP through a PAT connection exposed to an ACP session (agent lists issues; PAT never appears in the runner) — commits `feat(mcp): the gateway core — an upstream client, a grant's allow-list, and audited calls`, `feat(mcp): /mcp/{connectionId}, and an ACP session that calls a provider's tools` (ADR-0083; apps/api/test/mcp-gateway.test.ts: an ACP agent lists and calls a tool through the gateway, the provider sees only its PAT and the runner only Perch's token, an allow-list refuses a tool before the upstream is touched, and both outcomes are audited; packages/connect/test/connect.test.ts; apps/api/test/mcp-token.test.ts. The upstream is a stand-in on localhost — this environment cannot reach api.githubcopilot.com)
- [x] **1.18** Preview v1: port discovery, path-mode proxy, wildcard mode behind Caddy, WS passthrough, Preview tab (URL bar, viewport presets, reload, open in new tab), preview block in project config, share links (Vite HMR works in both modes; a share link opens without the inspector) — commits `feat(preview): a dev server on a runner port, reachable through Perch`, `perf(ui): split the message catalog so the first paint carries the shell's words only`, `feat(preview): the Preview tab, a member's ticket, and share links` (ADR-0084 and ADR-0085; e2e/preview.e2e.ts runs a real Vite 8 dev server through the proxy in both modes, edits a module and proves the hot update arrived over a socket on the preview's own hostname — a build with the passthrough disabled fails that assertion — then shares a link a signed-out browser opens and revokes it; apps/api/test/preview.test.ts covers both doors, the HMR socket, membership, share and revoke; packages/preview/test/preview.test.ts covers routing, headers, redirects and share rules. Path-mode HMR needs the dev server to know its prefix, per ADR-0084)
- [x] **1.19** Preview tunnel over local runners via http.open (HMR through a laptop runner from a phone) — commit `feat(preview): http.open, so a laptop's dev server is watchable from anywhere` (ADR-0086; apps/api/test/preview-tunnel.test.ts connects a runner the way `perch runner connect` does, with no preview host, and pulls a page, a 200 KB body, a binary body and a relayed HMR socket back through its own connection; packages/events/test/http-tunnel.test.ts covers the framing, bytes included; the phone half is e2e/preview.e2e.ts at 390 px)
- [x] **1.20** Git panel: status, stage, commit with AI message, branch, push, Open PR (commit and PR from the panel) — commit `feat(git): the Git panel — what changed, a written message, a branch, a push` (ADR-0087; e2e/git.e2e.ts edits a file, opens the drawer's Git tab, has the agent write the message, commits, and branches, at both viewports; apps/api/test/git-panel.test.ts covers status, the drafted message, commit, diff and branches against a real repository; the push and pull-request halves are the credential path connections.test.ts already drives against a stand-in GitHub)
- [x] **1.21** Laptop mode parity: perch dev runs 1.4–1.20 in-process on PGlite (laptop smoke extended to a full session) — commit `test(cli): laptop mode runs all of Phase 1, proven against a real perch dev` (ADR-0088; apps/cli/test/parity.test.ts spawns `perch dev`, completes the wizard, and drives a project, the fs, git with a drafted message, a session with a permission and its diff, ⌘K, a brain against a stand-in provider, a connection, the MCP gateway's challenge, a preview and a share link, and a terminal — on Linux, macOS and Windows in CI; the laptop it simulates declares its own PATH, and spikes/opencode no longer leaves the test process' PATH changed)
- [x] **1.22** Phase 1 e2e: the exit criterion as one Playwright spec on a paid key, on Ollama, through OpenCode, and through ACP (four green runs) — commit `test(e2e): Phase 1's exit criterion, on a key, on Ollama, through OpenCode, and through ACP` (ADR-0089; e2e/phase1.e2e.ts runs clone via GitHub → ask for a change → watch it in Preview from a phone → review the diff → commit → pull request as the projects phase1-key, phase1-ollama, phase1-opencode and phase1-acp, against a stand-in GitHub per lane, an OpenAI-shaped provider, a real Vite dev server and a stand-in `opencode serve`; it needed cloning through a connection in Code mode and an API base on a connection, both of which the api already had)

## Phase 2 — Chat, bots, the loop's front half

- [x] **2.1** Channels: public, private, DMs, groups, item threads; membership; header; archive; sidebar sections with unread weight (Playwright: create, join, leave, archive) — commit `feat(chat): channels — public, private, who is in them, and putting one away` (ADR-0090; channels routes and service over the 0.5 schema, Home's sidebar sections weighted by unread, the browser and the create form in main, a channel's header with topic, join, leave and archive; e2e/channels.e2e.ts drives create/join/leave/archive at both viewports with a second browser)
- [x] **2.2** Messages: blocks, edit history, delete, threads with reply counts, hover toolbar, pins, bookmarks, read state, mentions with autocomplete for people, bots, groups, channels, work items, files (Playwright covers each) — commit `feat(chat): messages — blocks, threads, pins, saves, edits and mentions` (ADR-0092; messages routes and service, migration 0013 for message_edits, the transcript with its hover toolbar and thread, the Composer's mention list, read state; e2e/messages.e2e.ts covers each at both viewports with two browsers)
- [x] **2.3** Reactions, files with previews, unfurls for Perch identifiers, web push (push arrives on a phone for a mention) — commit `feat(chat): reactions, files with previews, unfurls, and a mention that reaches a phone` (ADR-0093; reactions on the 0.5 schema with `reaction.*` events, files on PERCH_FILES_DIR served inert with image-only previews, an unfurl endpoint that answers only with what the caller may see, and web push — RFC 8291 and RFC 8292 in WebCrypto, migration 0014 for push_subscriptions, a service worker, and the bus subscriber that turns a mention into a notification; apps/api/test/{reactions,files,unfurls,push-crypto,push}.test.ts and e2e/{reactions-files-unfurls,push}.e2e.ts)
- [x] **2.4** Search: full-text over messages and files with filters; results page with peek (under 150 ms on 100k messages) — commit `feat(chat): search over what was said and what was attached` (ADR-0094; `/api/workspaces/{ws}/search` over the 0.5 tsvector with rank-then-fetch, file matching by name, the Search mode with marked results and a peek, migration 0015 for the blocks containment index that also makes a file as private as the message it was said in; apps/api/test/search.test.ts seeds 100k messages and asserts the slowest of three searches under 150 ms, e2e/search.e2e.ts covers the page at both viewports)
- [x] **2.5** Interactive blocks: BlockRenderer for button, select, form, approve_deny, progress; interaction.received delivery; in-place update (a bot's approve button updates the message and the bot gets the payload) — commit `feat(chat): interactive blocks — a question answered in the message itself` (ADR-0095; `state` on the four interactive block shapes, `POST …/messages/{m}/interactions` writing the answer into the block with no edit history and publishing `message.updated`, the Bot API event seam `createBotEvents()` in packages/bots carrying §7.3's `interaction.received`, and `BlockRenderer` at `@perch/ui/blocks` drawing all five kinds and their answered states; apps/api/test/interactions.test.ts, packages/bots/test/events.test.ts, packages/ui/src/components/blocks.ct.tsx and e2e/interactive-blocks.e2e.ts at both viewports)
- [x] **2.6** Native bot runtime: triggers, tool registry (web_search, http_fetch, chat_post, chat_read, remember, recall, thread_facts), streaming replies with placeholder edit, budgets, rate limits (a template bot answers a mention within budget) — commits `feat(bots): the bot schema, a brain that can be called, and the runtime that answers` and `feat(bots): a bot in a channel, answering` (ADR-0096; migration 0016 for bots/bot_installs/bot_runs/bot_memories, `@perch/gateway` building a model from a profile, `@perch/bots` with triggers, the native tool registry over a host interface, budgets and one streamed turn, and the api's side — the bus subscriber that fires triggers, the placeholder a reply fills in, `/api/workspaces/{ws}/bots` with install/test/runs, and bots as named members of a channel; apps/api/test/bots.test.ts runs the whole lane against a stub OpenAI-compatible endpoint, packages/bots/test/*.test.ts cover the runtime and every trigger, e2e/bots.e2e.ts answers a mention in the browser at both viewports)
- [x] **2.7** Bot-to-bot mentions: mention, wait_for_replies, hand_off, chain tracking, hop limit, repeat-pair breaker, per-thread budget, ChainHeader, intervene card (three bots complete a fan-out; a ping-pong pair trips the breaker) — commit `feat(bots): bots tagging bots, on rails` (ADR-0097; migration 0017 for bot_chains, the rails as pure functions in packages/bots, the mention/wait_for_replies/hand_off tools, one enforcement point where a trigger is matched, the breaker pausing a thread with an intervene card made of interactive blocks, `/stop` and `/resume`, `GET …/messages/{m}/chain` and the ChainHeader in the thread; packages/bots/test/chains.test.ts, apps/api/test/chains.test.ts covers the fan-out and the ping-pong acceptance, chain-header.ct.tsx and e2e/chains.e2e.ts at both viewports)
- [x] **2.8** Forge UI level 1: form, live test chat, publish to channels, the six templates; bot skills/ in the Agent Skills format (create Grok Newsroom; @grok answers in #general) — commit `feat(web): the Forge — six templates, a form, and a test chat` (ADR-0098; `@perch/bots/templates` as plain data so the browser never loads the runtime, the Forge as a section of workspace settings with install, pause and the ledger, `spec.skills` in the Agent Skills shape put in front of the model with the persona; packages/bots/test/templates.test.ts and e2e/forge.e2e.ts, which creates Grok Newsroom from the template and has it answer in #general at both viewports)
- [x] **2.9** DM-a-bot with model picker and New chat threads (fresh context per thread) (open the chat with @ada from Home, New chat starts it on nothing, and the brain is the person's to pick) — commit `feat(chat): chat with a bot, one fresh chat at a time` (ADR-0099; the room is an ordinary DM and every chat in it is a thread, so no migration; `POST …/bots/{bot}/dm` opens it and `PATCH …/bots/{bot}/install/{channel}` picks the brain, kept on the install because a DM is one person's room; `spec.brain.pick` is what offers the choice, and a bot now sees the thread it was asked in — its root included; apps/api/test/bot-dm.test.ts, apps/web/test/chats.test.ts and e2e/bot-chat.e2e.ts at both viewports)
- [x] **2.10** Inbox: inbox_items from permissions, chains, budgets, mentions; InboxList, resolve, snooze, batch approve; mobile launch tab (approve a session permission from the inbox on a phone) — commit `feat(inbox): one queue for everything waiting on you` (ADR-0100; migration 0018 for inbox_items with a payload column so a queue is one query and a unique ref index so an event twice is one row, a bus subscriber beside push and audit, `/api/inbox` with resolve and snooze, `InboxList` at `@perch/ui/inbox` answering permissions through the session's own endpoint, the four sidebar sections as one URL filter, and Inbox as the phone's launch tab; apps/api/test/inbox.test.ts, inbox-list.ct.tsx and e2e/inbox.e2e.ts at both viewports)
- [x] **2.11** Policy engine v1: policy.yaml schema, evaluator, protected branches, denied commands, path lists, model allow-lists per channel and project, budget ceilings, violation cards, dry-run endpoint (a local-only channel refuses a cloud profile; git push --force is blocked) — commit `feat(policy): one document says what may happen here` (ADR-0101; the evaluator and the merge in @perch/policy, migration 0019 keeping each document as the YAML it was written in, the api enforcing models per channel, ceilings on a bot's budget, the chain rails and protected branches on push while the runner keeps its own floor, `.../policy` and `.../policy/evaluate` with the dry run in Settings → Policy; packages/policy/test/document.test.ts, apps/api/test/policy.test.ts and e2e/policy.e2e.ts at both viewports)
- [x] **2.12** Secret scanning on agent diffs before commit (a planted key blocks the commit with a card) — commit `feat(policy): nothing is committed before it has been read for secrets` (ADR-0102; the scanner in @perch/policy with the provider prefixes, private keys, connection strings and a generic assignment rule, placeholders and example files left alone, removals ignored; the commit route reads the whole change — untracked files included, which is the case that matters — and refuses with a 451 whose findings carry a mask rather than the value; `secrets.*` in the policy document and a card in the Git panel; packages/policy/test/secrets.test.ts, the new case in apps/api/test/git-panel.test.ts and e2e/secrets.e2e.ts at both viewports)
- [x] **2.13** Project env and secrets: encrypted env, injection into runner, sessions, previews (a session sees DATABASE_URL; it never appears in a transcript) — commit `feat(projects): a project's environment, write-only and out of the transcript` (ADR-0103; project_env sealed per project and key with no route that returns a value, `session.create` and `pty.open` carrying it — the latter additive to §7.6 — the model credential layered on top so a project variable cannot stand in for a provider key, and redaction at SessionService.record() where every event passes; apps/api/test/project-env.test.ts asks the fixture agent to print the value and reads the redaction back, e2e/project-env.e2e.ts does the same in a browser at both viewports)
- [x] **2.14** Connections v2: MCP OAuth with CIMD, DCR, pre-registered; BYO OAuth wizard; Vercel, Supabase, Clerk connectors; connection test; grants UI; on-behalf-of rule (Supabase MCP connects via DCR; a shared bot is refused a personal connection) — commit `feat(connections): sign in through an MCP server, and lend one only on your behalf` (ADR-0104, ADR-0105; discovery is asked for when Connect is clicked rather than written into a manifest, the client lanes tried pre-registered → CIMD → RFC 7591, PKCE with an RFC 8707 resource indicator, and a personal connection granted to a workspace-visible bot refused at both the grant and the call; apps/api/test/connections-mcp.test.ts and packages/connect/test/discovery.test.ts cover the protocol, e2e/connections.e2e.ts runs the whole round-trip in a browser at both viewports against the stand-in MCP server scripts/e2e-server.ts starts)
- [x] **2.15** Deploy button and DB panel: Vercel deploy from the IDE with preview URL card; Supabase schema browser, read-only by default (deploy posts its preview URL in a thread) — commit `feat(deploy): ship from the drawer, and the card in the thread is the deploy` (ADR-0106; no deployments table — the `deploy_card` block posted in a channel is the record and is rewritten in place as the build moves, the database panel is the MCP gateway with two tool names off the connector manifest, read-only enforced in Perch before the provider is asked, and `PATCH .../projects/{p}` so a project pushed somewhere after it was made can say where; apps/api/test/deploys.test.ts and e2e/deploy.e2e.ts at both viewports against the stand-in Vercel and Supabase scripts/e2e-server.ts starts)
- [x] **2.16** Inspector v1: @perch/inspector plugins (Vite, Next, Webpack), injected client, Elements tree, select → context chip, console and failed requests strip with Send to agent, screenshot to thread via Playwright in the runner (click a button → "make this primary" lands the right edit) — commit `feat(inspector): point at the thing you mean, and the agent edits that file` (ADR-0107, ADR-0108; the source tag is a scanner rather than a parser so the plugins depend on nothing, the client is injected by the proxy only for a member's own pane and nonce-bound, context chips ride one turn and are spent, and `preview.screenshot` is additive to §7.6; packages/inspector and packages/preview cover the transform, the injection and the CSP, e2e/inspector.e2e.ts runs the acceptance in a browser at both viewports)
- [x] **2.17** Repo intelligence v1: repo_index chunking and embeddings on a job, @codebase context, semantic search across code and chat, AGENTS.md draft generator (an @codebase question cites the right file) — commit `feat(repo-index): Perch reads a project once, and @codebase cites the file` (ADR-0110; the embedding is optional and named by a model profile whose `default_for` is the new `embedding`, so an instance with no credentials still has a words-only index that cites the right file, and `fit()` brings any provider's width to the column's 1024; the `@codebase` block rides beside the turn rather than inside it, so the transcript keeps what the person typed and the §7.6 engine seam is unchanged; chunking is `packages/repo` — a symbol chunk per declaration, a 60-line window otherwise, Markdown at its headings — indexing runs on the new `repo-index` queue, and the search box gains a Code lane across every project; apps/api/test/repo-index.test.ts holds the acceptance from the model's side and e2e/repo-index.e2e.ts runs it in a browser at both viewports)
- [ ] **2.18** Custom actions, reasoning level, background and auto-settle policies per project (a custom action runs from the session pane and from ⌘K)
- [ ] **2.19** Bot API v1: endpoints and socket mode per §7.3 with scopes and rate limits; bot-sdk published (an external script posts a message and receives an app_mention)
- [ ] **2.20** Perf audit script in CI: WS payload budget, bundle size budget, list virtualization check (budgets enforced)
- [ ] **2.21** Phase 2 e2e: the exit criterion as Playwright specs; axe sweep on Home, Code, Inbox (all green)

## Phase 3 — Bot platform + the loop

Written at the Phase 2 gate (spec §10) and reviewed by the human before starting.

## Phase 4 — Gateway, hardening, launch

Written at the Phase 3 gate (spec §10) and reviewed by the human before starting.

## Beyond the spec

- [x] **D.1** Desktop app: `apps/desktop` / `perch-desktop`, laptop mode in a native window on macOS,
  Windows, and Linux, built per platform by the release workflow — commit `feat(desktop): the Perch
  desktop app` (ADR-0063; apps/desktop/test/desktop.test.ts, the laptop-smoke desktop steps)
