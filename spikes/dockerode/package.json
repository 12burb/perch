{
  "name": "@perch/spike-dockerode",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 0 spike: dockerode from Bun creates, limits, execs into, and removes a runner container",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/dockerode": "4.0.1",
    "dockerode": "5.0.1"
  }
}
