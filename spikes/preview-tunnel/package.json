{
  "name": "@perch/spike-preview-tunnel",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 0 spike: Vite HMR end to end through the api over a local runner's outbound WebSocket",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "vite": "8.3.0"
  }
}
