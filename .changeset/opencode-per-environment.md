---
"@perch/runner": patch
---

OpenCode sessions run on one server per project directory *and* environment: two people on the
same project, or one person switching brains, no longer share the first session's credentials and
HOME (ADR-0161).
