---
"@perch/api": patch
---

`docs/publishing.md`: what is left to do by hand for the install lanes that need a credential or a
setting — npm, the docs site, the AUR and winget — one runbook each, with what to check at the end.
